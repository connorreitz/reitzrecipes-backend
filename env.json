{
    "Parameters": {
        "NODE_ENV": "localhost",
        "NODE_CONFIG_DIR": "./config",
        "AWS_REGION": "us-east-2",
        "AWS_PROFILE": "reitzrecipes-dev",
        "S3_BUCKET_NAME": "reitz-recipes-templates"
    }
}